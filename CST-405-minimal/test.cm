int adding(int x, int y) {
    int result;
    result = x + y;
    return result;
}

float multiply(float a, float b) {
    return a * b;
}

void main() {
    int sum;
    sum = multiply(5, 3);
    print(sum);
}
