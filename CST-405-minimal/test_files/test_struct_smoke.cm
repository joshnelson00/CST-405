struct Point { int x; int y; };

int sum(struct Point *p) {
    return p.x + p.y;
}

int main() {
    struct Point a;
    struct Point *pa;
    int s;

    a.x = 4;
    a.y = 7;
    pa = &a;
    s = sum(pa);

    print(s);
    print(pa.x);
    return 0;
}
