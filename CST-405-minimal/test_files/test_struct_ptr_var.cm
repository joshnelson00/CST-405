struct Pair { int a; int b; };

int sum(struct Pair *p) {
    return p.a + p.b;
}

int main() {
    struct Pair q;
    struct Pair *ptr;
    q.a = 4;
    q.b = 5;
    ptr = &q;
    print(sum(ptr));
    print(ptr.a);
    return 0;
}
